@model Web.Models.ProtectionViewModel
@{
    ViewBag.Title = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="col-md-6">
    <div class="box box-info">
        <div class="box-header with-border">
            <h3 class="box-title">Yeni Erişim Noktası Oluştur</h3>
            <h3 class="box-title">@Html.ValidationSummary()</h3>
        </div>
        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()
            <div class="form-horizontal">
                <div class="box-body">
                    <div class="form-group">
                        <label for="inputEmail3" class="col-sm-2 control-label">@Html.LabelFor(x => x.roles)</label>
                        <div class="col-sm-10">
                            <select name="role_id" class="form-control">
                                @foreach (var item in Model.roles)
                                {
                                    <option value="@item.Id">@item.Name</option>
                                }
                            </select>
                        </div>
                    </div>
                    <div class="form-group">
                        <label for="inputPassword3" class="col-sm-2 control-label">@Html.LabelFor(x => x.menu)</label>
                        <div class="col-sm-10">
                            <select name="menu_id" class="form-control">
                                @foreach (var item in Model.menu)
                                {
                                    <option value="@item.Id">@item.Name</option>
                                }
                            </select>
                        </div>
                    </div>
                </div>
                <div class="box-footer">
                    <a href="/Server/Index" class="btn btn-primary">Vazgeç</a>
                    <button type="submit" class="btn btn-success pull-right">Ekle</button>
                </div>
            </div>
        }
    </div>
</div>
