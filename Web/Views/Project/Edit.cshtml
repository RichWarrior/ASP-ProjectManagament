@model Web.Models.ProjectViewModel
@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="col-md-6">
    <div class="box box-info">
        <div class="box-header with-border">
            <h3 class="box-title">Yeni Proje Oluştur</h3>
            <h3 class="box-title">@Html.ValidationSummary()</h3>
        </div>
        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()
            <div class="form-horizontal">
                <div class="box-body">
                    <div class="form-group">
                        <label for="inputEmail3" class="col-sm-2 control-label">@Html.LabelFor(x => x.Name) (*)</label>
                        <div class="col-sm-10">
                            <input type="text" name="Name" class="form-control" value="@Html.ValueFor(x=>x.Name)" />
                        </div>
                    </div>
                </div>
                <div class="box-footer">
                    <a href="/Project/Index" class="btn btn-primary">Vazgeç</a>
                    <button type="submit" class="btn btn-warning pull-right">Düzenle</button>
                </div>
            </div>
        }
    </div>
</div>

