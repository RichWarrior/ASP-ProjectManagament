@model IEnumerable<Web.Models.MenuViewModel>
@{
    ViewBag.Title = "SubMenu";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<section class="content">
    <div class="row">
        <div class="col-xs-12">
            <div id="modalDiv" class="box">
                <div class="box-header">
                    <h3 class="box-title">Alt Menüler Menüler</h3>
                </div>
                <div class="box">
                    <div class="box-header col-sm-3">
                        <a href="/Menu/CreateSubMenu?=@ViewBag.Id" class="btn btn-primary" style="margin-top:8px;">
                            <i class="fas fa-plus">
                                Yeni Alt Menü Tanımla
                            </i>
                        </a>                       
                        @Html.ValidationSummary()
                    </div>
                </div>
                <div class="box-body">
                    <table id="example1" class="table table-bordered table-striped">
                        <thead>
                            <tr>
                                <th>@Html.LabelFor(x => x.FirstOrDefault().Name)</th>
                                <th>@Html.LabelFor(x => x.FirstOrDefault().Icon)</th>
                                <th>@Html.LabelFor(x => x.FirstOrDefault().Rank)</th>
                                <th></th>
                            </tr>
                        </thead>
                        <tbody id="container">
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>@item.Name</td>
                                    <td><i class="@item.Icon"></i></td>
                                    <td>@item.Rank</td>
                                    <td>
                                        <a href="/Menu/Edit?id=@item.Id" class="btn btn-primary">
                                            <i class="fas fa-pencil-alt">
                                                Düzenle
                                            </i>
                                        </a>
                                        <a href="/Menu/Delete?id=@item.Id" class="btn btn-primary">
                                            <i class="fas fa-trash">
                                                Sil
                                            </i>
                                        </a>                                        
                                    </td>
                                </tr>
                            }
                        </tbody>
                        <tfoot>
                            <tr>
                                <th>@Html.LabelFor(x => x.FirstOrDefault().Name)</th>
                                <th>@Html.LabelFor(x => x.FirstOrDefault().Icon)</th>
                                <th>@Html.LabelFor(x => x.FirstOrDefault().Rank)</th>
                                <th></th>
                            </tr>
                        </tfoot>
                    </table>
                </div>
            </div>
        </div>
    </div>
</section>

